# Recommended: fill in one API key and start the app.
KALEIDOSCOPE_AI_PROVIDER=deepseek
DEEPSEEK_API_KEY=

# Current official models: deepseek-v4-flash (faster) or deepseek-v4-pro.
DEEPSEEK_MODEL=deepseek-v4-flash

# Optional bounded response timeout, clamped to 15–300 seconds.
DEEPSEEK_TIMEOUT_MS=120000

# Alternative local provider: reuse the signed-in Codex CLI.
# KALEIDOSCOPE_AI_PROVIDER=codex

# Optional when the packaged app cannot resolve `codex` from PATH.
# Set this at launch time with: KALEIDOSCOPE_CODEX_PATH="$(command -v codex)"
KALEIDOSCOPE_CODEX_PATH=

# Optional bounded response timeout, clamped to 15–600 seconds.
KALEIDOSCOPE_CODEX_TIMEOUT_MS=180000

# Optional override for the repository-owned standard knowledge base.
# By default development uses content/ods-material/knowledge_base.
KALEIDOSCOPE_KNOWLEDGE_BASE_PATH=

# Deterministic, offline test mode:
# KALEIDOSCOPE_AI_PROVIDER=demo
